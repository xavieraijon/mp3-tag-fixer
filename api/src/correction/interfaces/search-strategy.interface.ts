/**
 * @deprecated Use SearchStrategy from '@shared/interfaces' instead.
 * This file is kept for backwards compatibility during migration.
 * Re-exports the centralized SearchStrategy interface.
 */
export type { SearchStrategy } from '../../shared/interfaces/search-strategy.interface';
